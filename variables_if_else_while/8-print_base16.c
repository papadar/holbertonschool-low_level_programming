#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: text here
 *
 * Return: Always returns 0
 */
int main(void)
{
	int i;

	for (i = '0'; i < 16; i++)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);
}
