#include "main.h"

/**
 * main - Entry point
 *
 *Description: print the alphabet using only two _printchars
 *
 *Return: Always returns 0
 */
int main(void)
{
	char c;
	char z;

	c = 'a';
	z = 'z';

	while (c <= z)
	}
		_putchar(c);
		c++;
	{
	_putchar('\n');

	return (0);
}
