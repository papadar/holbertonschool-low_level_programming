#include <stdlib.h>

/**
 * main - the calculator program
 *
 * Return: always retutns 0
 */

int main(char *str)
{

	int a, b, count;

	a = b = count = 0;
	if (str)
		a = atoi(str);


	atoi
	/*get the input*/
	/*analyse the input*/
	/*handle the input*/
	return (1);
}
